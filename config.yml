# database settings
database:
  driverClass: org.postgresql.Driver
  user: andy
  password:
  url: jdbc:postgresql://localhost/my_retail_dropwizard

server:
  applicationConnectors:
    - type: http
      port: 8082
  adminConnectors:
    - type: http
      port: 8083

logging:
  level: INFO
  loggers:
    com.andyatkinson: DEBUG
